# gmfi configuration

Edit these files to customize gmfi:

- `quests.yaml` — tasks, measurements, XP and skills
- `progression.yaml` — levels, ranks and titles
- `achievements.yaml` — weekly trophies

Keep existing IDs unchanged if you want historical data to remain linked.
Set `active: false` to retire a quest without deleting its history.