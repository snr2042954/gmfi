services:
  gmfi:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: gmfi

    restart: unless-stopped

    ports:
      - "${TAILSCALE_IP}:${PORT}:${PORT}"

    env_file:
      - .env

    volumes:
      - ./data:/app/data